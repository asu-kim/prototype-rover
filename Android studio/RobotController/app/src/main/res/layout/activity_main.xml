<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:gravity="center">

    <!-- Title -->
    <TextView
        android:id="@+id/tv_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Robot Control Panel"
        android:textSize="20sp"
        android:layout_marginBottom="8dp"
        android:textStyle="bold" />

    <!-- Subtitle -->
    <TextView
        android:id="@+id/tv_subtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Enter the IP Address and Control the Robot"
        android:textSize="14sp"
        android:layout_marginBottom="16dp" />

    <!-- IP Address Input -->
    <EditText
        android:id="@+id/et_ip_address"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter Raspberry Pi IP Address"
        android:inputType="text"
        android:padding="8dp"
        android:layout_marginBottom="16dp" />

    <!-- Connect Button -->
    <Button
        android:id="@+id/btn_connect"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Connect"
        android:layout_marginBottom="16dp" />

    <!-- Control Buttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center">

        <Button
            android:id="@+id/btn_forward"
            android:layout_width="200dp"
            android:layout_height="wrap_content"
            android:text="Forward"
            android:layout_marginBottom="10dp"
            android:enabled="false" />

        <Button
            android:id="@+id/btn_backward"
            android:layout_width="200dp"
            android:layout_height="wrap_content"
            android:text="Backward"
            android:layout_marginBottom="20dp"
            android:enabled="false" />
    </LinearLayout>

    <!-- Steering Control -->
    <TextView
        android:id="@+id/tv_steering_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Steering Control (Direction)"
        android:textSize="14sp"
        android:layout_marginBottom="8dp"
        android:visibility="visible" />

    <SeekBar
        android:id="@+id/slider_steering"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:max="100"
        android:progress="50"
        android:visibility="visible" />

    <!-- Action Buttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginTop="16dp">

        <Button
            android:id="@+id/btn_start_recording"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Start Recording"
            android:layout_margin="8dp"
            android:enabled="false" />

        <Button
            android:id="@+id/btn_stop_recording"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Stop Recording"
            android:layout_margin="8dp"
            android:enabled="false" />
    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginTop="8dp">

        <Button
            android:id="@+id/btn_ignore_obstacles"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Ignore Obstacles (5s)"
            android:layout_margin="8dp"
            android:enabled="false" />

        <Button
            android:id="@+id/btn_emergency_stop"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Emergency Stop"
            android:layout_margin="8dp"
            android:enabled="false" />
    </LinearLayout>
</LinearLayout>
